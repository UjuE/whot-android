<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pink.digitally.games.whot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".view.PlayerRegistrationActivity"
            android:label="@string/title_activity_player_registeration">

            <intent-filter>
                <action android:name="pink.digitally.games.whot.intent.action.PLAYERS_REGISTRATION" />
                <category android:name="pink.digitally.games.whot.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.DigitallyPinkGamesSplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity">
<!--            android:label="@string/app_name"-->
<!--            android:theme="@style/AppTheme.NoActionBar">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>

        <activity android:name=".view.PlayGameActivity" android:label="@string/title_play_game">
            <intent-filter>
                <action android:name="pink.digitally.games.whot.intent.action.PLAY_GAME" />
            </intent-filter>
        </activity>
    </application>

</manifest>